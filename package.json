{
  "name": "root",
  "private": true,
  "scripts": {
    "build:all": "npx father build",
    "format": "prettier --write \"**/*.js\""
  },
  "devDependencies": {
    "lerna": "^3.18.4",
    "prettier": "^1.19.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "immer": "^5.0.0"
  }
}
